# 基础依赖
opencv-python>=4.5.0
numpy>=1.20.0
matplotlib>=3.3.0
pillow>=8.0.0

# UI 界面（必需）
ttkbootstrap>=1.10.0

# 说明：
# - 本项目推荐使用 conda 环境（environment.yml）以获得更好的稳定性
# - 如果使用 pip 安装，请确保 Python 版本 >= 3.8
# - Windows 用户推荐使用 conda 以避免 OpenCV/NumPy 兼容性问题